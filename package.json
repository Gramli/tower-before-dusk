{
  "name": "tower-before-dusk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "agent": "tsx playwright-agent/agent.ts",
    "agent:local": "tsx playwright-agent/agent.ts http://localhost:5173",
    "agent:prod": "tsx playwright-agent/agent.ts https://tower-before-dusk.gramli.workers.dev"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "@types/node": "^26.0.0",
    "dotenv": "^17.4.2",
    "playwright": "^1.61.1",
    "tsx": "^4.22.4",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "wrangler": "^4.103.0"
  },
  "dependencies": {
    "@google/genai": "^2.10.0"
  }
}
